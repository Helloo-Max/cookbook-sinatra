<p> What recipe would you like to import? </p>

<ul>
  <% @results.each_with_index do |recipe, index| %>
  <li>
    <% index + 1 %> |
    <%= recipe[:name] %> |
    <%= recipe[:description] %> |
    <%= recipe[:rating] %> / 5 ⭐️ |
    <%= recipe[:prep_time] %> ⏲

    <a href="/import/<%= @query %>/<%= index %>">Import</a> |
  </li>
  <% end %>
</ul>
